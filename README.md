# Habit Tracker

A SwiftUI iOS app for tracking daily habits.

## Features
- Add and track habits
- Native SwiftUI interface

## Requirements
- Xcode
- iOS (SwiftUI)

## Running
Open `HabitTracker.xcodeproj` in Xcode and run on a simulator or device.
